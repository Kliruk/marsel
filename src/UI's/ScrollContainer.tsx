import {IScrollContainer} from "@/types/scroll-container";
import React from "react";


/**
 *
 * @return {JSX.Element}
 */
const ScrollContainer = ({uniqueClassName, data, width, height} : IScrollContainer)
: JSX.Element => {
  return (
    <div>Scroll</div>
  );
};

export default ScrollContainer;
